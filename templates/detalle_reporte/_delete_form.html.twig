<form method="post" action="{{ path('detalle_reporte_delete', {'id': detalle_reporte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detalle_reporte.id) }}">
    <button class="btn">Delete</button>
</form>
