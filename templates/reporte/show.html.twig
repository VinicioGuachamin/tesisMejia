{% extends 'base.html.twig' %}

{% block title %}Reporte{% endblock %}

{% block body %}
    <h1>Reporte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reporte.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ reporte.fecha ? reporte.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Inspector</th>
                <td>{{ reporte.inspector }}</td>
            </tr>
            <tr>
                <th>Jornada</th>
                <td>{{ reporte.jornada }}</td>
            </tr>
            <tr>
                <th>Grado</th>
                <td>{{ reporte.grado }}</td>
            </tr>
            <tr>
                <th>Paralelos</th>
                <td>{{ reporte.paralelos }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reporte_index') }}">back to list</a>

    <a href="{{ path('reporte_edit', {'id': reporte.id}) }}">edit</a>

    {{ include('reporte/_delete_form.html.twig') }}
{% endblock %}
